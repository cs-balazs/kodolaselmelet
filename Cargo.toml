[package]
name = "kodolaselmelet"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rug = { version = "1.17", features = ["integer", "rand"] }
rand = "0.8"
lazy_static = "1.4.0"
